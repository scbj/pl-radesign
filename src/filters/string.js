/**
 * Rreturns the calling string value converted to uppercase
 * (the value will be converted to a string if it isn't one)
 * @returns {String}
 */
export function upperCase (value) {
  return value.toUpperCase()
}
